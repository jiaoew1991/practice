Import('env')
Import('libs')

build_env = env.Clone()

env.Program(target='arraylist_unittest', source=['arraylist_unittest.cpp'], LIBS=libs)
env.Program(target='linkedlist_unittest', source=['linkedlist_unittest.cpp'], LIBS=libs)
env.Program(target='binarysearchtree_unittest', source=['binarysearchtree_unittest.cpp'], LIBS=libs)
env.Program(target='avlsearchtree_unittest', source=['avlsearchtree_unittest.cpp'], LIBS=libs)